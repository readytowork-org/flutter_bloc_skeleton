import 'package:flutter_bloc_skeleton/core/utils/enum/index.dart';

class Config {
  static const baseUrl = "url";
  static const locale = "en";
  static const flavour = Flavour.production;
  static const cacheDays = 2;
}
